{% extends 'events/details.html.twig' %}
{% block title %}Edit{% endblock %}
{% block extra %}
   <div class="d-flex flex-column justify-content-between align-items-center">
      <h2 class="my-5">Edit Event</h2>
      <div class="w-75">
         {{form_start(form)}}
         {{form_widget(form, {'attr': {'class': 'd-flex flex-wrap justify-content-between'}})}}
         {{form_end(form)}}
      </div>
   </div>
{% endblock %}